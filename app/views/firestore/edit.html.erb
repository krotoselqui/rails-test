<div class="max-w-2xl mx-auto p-6">
  <% flash.each do |name, msg| %>
    <div class="mb-4 p-4 rounded-lg bg-yellow-100 text-yellow-700">
      <%= msg %>
    </div>
  <% end %>

  <h1 class="text-2xl font-bold mb-6">メモの編集</h1>

  <%= render 'form' %>

  <div class="mt-6">
    <%= link_to "戻る", firestore_index_path, class: "text-indigo-600 hover:text-indigo-800" %>
  </div>
</div>