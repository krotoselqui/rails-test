<div class="max-w-2xl mx-auto p-6">
  <% flash.each do |name, msg| %>
    <div class="mb-4 p-4 rounded-lg bg-yellow-100 text-yellow-700">
      <%= msg %>
    </div>
  <% end %>

  <h1 class="text-2xl font-bold mb-6">新規メモ作成</h1>

  <%= render 'form' %>
</div>